name: CI

on:
  push:
  pull_request:

jobs:
  validate-shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check shell scripts
        run: bash -n scripts/*.sh
